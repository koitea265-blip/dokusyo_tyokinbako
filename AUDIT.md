# ✅ GIGA Standard v4 監査：どくしょ ちょきんばこ（Reading-Books）

> **この文書は「改修に入る前の状態」の記録です。**
> ここに書かれた ❌ / ⚠️ に対して何をしたかは、末尾の
> 「改修後の状態」と [`ROLLOUT.md`](ROLLOUT.md) を見てください。
> 機械的な再チェックは `npm run check` で いつでも走らせられます。

- 監査日：2026-08-03
- 対象コミット：`a07db1f`（改修前）
- **判定：A型（単一HTML完結）** — `vite.config.*` なし／`.gs` なし／`manifest_version` なし
- 実測ファイル：`index.html`（2,847行 / 136KB）・`sw.js`（168行）・`studyLog.js`（97行）・`manifest.json`・`icons/`・`vendor/quagga.min.js`

判定記号：✅ 適合 ／ ⚠️ 部分的 ／ ❌ 不適合 ／ — 対象外

---

## A. 法務・配布

| # | 項目 | 判定 | 実測 | 対応 |
|---|---|:--:|---|:--:|
| A1 | LICENSE 実ファイル | ❌ | ファイルなし | P0 |
| A2 | .gitignore | ❌ | ファイルなし | P0 |
| A3 | dependabot.yml | ❌ | `.github/` 自体が存在しない | P0 |
| A4 | README.md / MANUAL.md 両方 | ⚠️ | README.md は充実（10.4KB・9節）。MANUAL.md なし | P3 |

## B. セキュリティ

| # | 項目 | 判定 | 実測 | 対応 |
|---|---|:--:|---|:--:|
| B1 | CSP（connect-src が最小） | ❌ | `Content-Security-Policy` の記述 0件 | P1-9 |
| B2 | 秘密情報・IDの直書きなし | ✅ | `.env`／`.clasp.json` はコミットされていない。APIキー不要の公開API（openBD / Google Books / NDL）のみ | — |
| B3 | OAuthスコープ最小 | — | GAS を使わない A型 | — |
| B4 | postMessage の宛先が `*` でない | ✅ | 該当箇所は SW → クライアントの `event.source.postMessage` のみ（宛先指定不要） | — |
| B5 | サーバー側5段ガード | — | サーバーを持たない。個人情報は端末内のみ | — |

**補足**：書誌APIとして `api.openbd.jp` / `www.googleapis.com` / `ndlsearch.ndl.go.jp` の3件に接続する。CSP の `connect-src` はこの3件に限定できる（ワイルドカード不要）。

## C. 堅牢性

| # | 項目 | 判定 | 実測 | 対応 |
|---|---|:--:|---|:--:|
| C1 | LockService + try/finally | — | GAS なし | — |
| C2 | 自動復旧 | ✅ | `studyLog.js` は壊れた JSON を検出したら空から再開する（97行目付近） | — |
| C3 | pagehide で記録確定 | ✅ | `index.html:2811` に `pagehide` ハンドラあり | — |
| C4 | 通信失敗時のリトライと明示 | ✅ | 書誌API は3系統フォールバック。失敗時は手入力へ誘導 | — |
| C5 | localStorage.clear() を使っていない | ✅ | 出現は 2572行のコメント（禁止事項の注意書き）のみ。実コードでの使用 0件 | — |

## D. 表示（Part I §2）

| # | 項目 | 判定 | 実測 | 対応 |
|---|---|:--:|---|:--:|
| D1 | viewport に viewport-fit=cover | ✅ | `index.html:5` に記載。`user-scalable=no` も付いていない（拡大可） | — |
| D2 | 100dvh を使用（100vh 単独でない） | ⚠️ | `100vh` の使用 0件。骨格は `.app{position:fixed;inset:0}` で組んであり実害はないが、`dvh` も 0件でフォールバック指定が無い | P1-1 |
| D3 | safe-area-inset を適用 | ✅ | 4箇所（`--safe-top/bottom/left/right`）。ヘッダー・下ナビ・左右パディングに反映済み | — |
| D4 | clamp() による fluid type | ⚠️ | `clamp()` は18箇所使用。ただし `--fs-body` は `15px` 固定、`line-height:1.5`（児童向けの推奨 1.8 に未達） | P1-5 |
| D5 | Canvas に devicePixelRatio 補正（上限2） | ✅ | `index.html:1136` `Math.min(devicePixelRatio, 2)`。座標をすべて dpr 倍して描画しており実質的に正しい。ただし `ResizeObserver` 追従なし（紙ふぶきは一瞬の演出なので実害なし） | — |
| D6 | 320px 幅で横スクロールが出ない | ⚠️ | 未検証（P1 で実測する） | P1-検証 |
| D7 | 画像に width/height、150KB以下 | ✅ | `<img>` タグ 0件（アイコンは全て SVG スプライト）。PNG は全て 150KB 未満 | — |
| D8 | コントラスト 4.5:1 以上 | ⚠️ | 主要な組み合わせは良好（`#14283C` on `#F3F0E9` ≒ 13:1、`#1F4C72` on 白 ≒ 8.9:1）。`--ink-faint:#9AA7B5` を白背景の小文字に使うと 2.4:1 で不足 | P1-要確認 |
| D9 | タップ領域 44px 以上・touch-action | ⚠️ | `touch-action` / `overscroll-behavior` / `tap-highlight` は適用済み。`.btn`(50px)・`.btn-xl`(64px) は十分だが、`.btn-sm{min-height:0}`・`.hdr-btn`(実測≒28px)・`.seg button` が 44px 未満 | P1-6 |
| D10 | prefers-reduced-motion 対応 | ⚠️ | `index.html:194` にブロックあり。ただし画面遷移アニメのみを止めており、`animation-duration`/`transition-duration` の全体無効化がない | P1-7 |
| D11 | 提示モード | — | 一人ひとりの読書記録アプリで、一斉授業での提示を前提としない。個人の読書履歴を電子黒板に映すことは個人情報の観点からも推奨されない | 対象外 |
| D12 | 印刷CSS | ❌ | `@media print` 0件。**読書記録は「印刷して残す・家庭に渡す」需要がある記録系アプリ**であり、本質機能として不足 | P1-新規 |

## E. PWA（Part I §3）

| # | 項目 | 判定 | 実測 | 対応 |
|---|---|:--:|---|:--:|
| E1 | manifest の id/scope/start_url がリポジトリ名絶対パス | ⚠️ | `id:"/Reading-Books/"` は ✅。`scope:"./"` `start_url:"./"` が**相対パス**。現在の配置では同じ場所に解決されるが、規約が求める明示性を満たさない | P1-8 |
| E2 | アイコン4種 + apple-touch-icon | ✅ | 192/512/maskable-192/maskable-512/apple-touch-icon-180/favicon-64/icon.svg すべて存在 | — |
| E3 | beforeinstallprompt を head 最上部で捕捉 | ❌ | **`index.html:2742`** で登録している。`<head>` 終端は 530行目付近なので、**2,200行以上あとの `<script>` で受けている**。通信の遅い端末やパース途中で合図を取りこぼし、「インストール」ボタンが出ない可能性がある | P1-8 |
| E4 | インストールボタンをアプリ内に設置 | ✅ | `#install-row`（744行）。`@media (display-mode:standalone)` で非表示化も実装済み | — |
| E5 | sw.js が自アプリ接頭辞のキャッシュのみ削除 | ✅ | `CACHE_PREFIX='reading-books-'` で絞り込み済み。他アプリを壊していない | — |
| E6 | sw.js が localStorage に触れていない | ✅ | 出現はコメント1行のみ | — |
| E7 | 更新通知 | ✅ | `updatefound` → トースト → `SKIP_WAITING`（2789行付近） | — |
| E8 | offline.html | ❌ | ファイルなし。SW は文字列 `<h1>オフラインです</h1>` を返すだけで、アプリと配色もフォントも違う | P1-8 |
| E9 | APP_VERSION を今回のリリース値に更新 | ⚠️ | `sw.js` と `index.html:941` の両方に `2.3.0`。今回の改修で上げる必要がある | P1-8 |
| E10 | iOS の「ホーム画面に追加」手順を MANUAL に記載 | ⚠️ | アプリ内には案内あり（2772行）。MANUAL.md が無いため文書としては未整備 | P3 |

## F. アクセシビリティ・性能

| # | 項目 | 判定 | 実測 | 対応 |
|---|---|:--:|---|:--:|
| F1 | alt / aria-label / aria-live | ⚠️ | `aria-label` 20箇所、`aria-live` 1箇所（トースト）、装飾SVGは `aria-hidden` 済み。`<img>` 無しのため alt 対象なし。**画面切替を読み上げる `aria-live` が無い** | P1-a11y |
| F2 | キーボードのみで全機能に到達 | ⚠️ | 操作は `<button>` 中心で概ね到達可能。モーダル（`.celebrate`）にフォーカストラップと Esc がない | P1-a11y |
| F3 | 初回JS 300KB以下 | ✅ | `vendor/quagga.min.js` 93KB + インライン JS 約 60KB ＝ **約 153KB** | — |
| F4 | 1ファイル 5,000行 / 400KB 以内 | ✅ | `index.html` 2,847行 / 136KB（上限内） | — |

**外部依存の実測**：`https://fonts.googleapis.com` から Zen Maru Gothic を読み込んでいる（`index.html:25`）。
A型の規約「CDN禁止・自己ホスト」に反し、**初回オフライン時と校内フィルタ配下でフォントが当たらない**。

## G. 学習ログ（study.v1）

| # | 項目 | 判定 | 実測 | 対応 |
|---|---|:--:|---|:--:|
| G1 | study.v1 準拠・個人情報を持たない | ✅ | `studyLog.js` はロジック版 1.1。氏名・出席番号・メールを持たない。外部送信なし | — |
| G2 | 中断記録・5分ルール | ✅ | `pagehide` で確定。読書記録という性質上「中断セッション」は発生しない設計 | — |

---

## ❌ の集計と対処方針

| 項目 | 何が起きるか（先生向けの説明） | フェーズ |
|---|---|---|
| A1 A2 A3 | 配布のライセンスが不明。依存ライブラリの脆弱性通知が届かない | P0 |
| E3 | 端末や通信状況によって「インストール」ボタンが出ないことがある | P1 |
| E8 | 圏外で開くと、アプリと全く違う真っ白な画面が出て「こわれた」と思われる | P1 |
| D12 | 読書の記録を紙に印刷して家庭に渡せない | P1 |
| B1 | 万一の改ざん時に、外部への通信を止める仕組みがない | P1 |
| F 外部依存 | 校内ネットワークやオフラインで、想定した丸ゴシック体にならない | P1 |
| A4 E10 | 先生が困ったときに読む手引きがない | P3 |

## 人間に確認したいこと

1. **`icon-512.png`（93KB）／`icon-maskable-512.png`（77KB）の圧縮**
   規約の目安は 60KB。圧縮すると画質がわずかに落ちる可能性があるため、before/after を出して確認をお願いする（P2）。
2. **`--ink-faint:#9AA7B5` の扱い**
   白背景の 11px 文字に使うとコントラストが 2.4:1 で不足する。色そのものは規約 §改修モード-6「配色を変更しない」の対象なので、**報告に留め、使用箇所の見直し案のみ提示**する。
3. **`manifest.json` のファイル名**
   規約は `manifest.webmanifest`。ただし既にインストール済みの端末が参照しているため、改名は行わず現状維持とする。

## 対象外と判断した項目

- **D11 提示モード**：個人の読書履歴を電子黒板に映す使い方を想定しない
- **B3 B5 C1**：サーバー（GAS）を持たない A型のため該当なし


---

# 📌 改修後の状態（2026-08-03 / P0〜P4 実施後）

`npm run check` の結果：**❌ 0 ／ ⚠️ 0 ／ － 3（対象外・理由つき許可）／ ✅ 54**

## 監査時の ❌ / ⚠️ が どうなったか

| # | 監査時 | 改修後 | 何をしたか |
|---|:--:|:--:|---|
| A1 A2 A3 | ❌ | ✅ | LICENSE / .gitignore / dependabot.yml を作成（P0） |
| A4 | ⚠️ | ✅ | MANUAL.md を作成。README に不足節を追記（P3） |
| B1 | ❌ | ✅ | CSP を投入。実機で `Refused to` 0件を確認（P1） |
| D2 | ⚠️ | ✅ | `100dvh`（`100vh` フォールバックつき）を追加（P1） |
| D4 | ⚠️ | ✅ | `--fs-body` を `clamp()` に。読ませる文章の行間を 1.8 に（P1） |
| D6 | ⚠️ | ✅ | 320 / 375 / 810 / 1366 / 1920px で横スクロールなしを実測（P1） |
| D8 | ⚠️ | ✅ | `--ink-faint` を 4.5:1 まで濃くした（利用者の許可を得て配色変更）。下記 |
| D9 | ⚠️ | ✅ | 押せる要素で 44px 未満のもの 0件を実測（P1） |
| D10 | ⚠️ | ✅ | すべてのアニメーション・トランジションを止める形に（P1） |
| D12 | ❌ | ✅ | A4たての印刷を追加。専用シートを組み立てて出す（P1） |
| E1 | ⚠️ | ✅ | `scope` / `start_url` をリポジトリ名の絶対パスに（P1） |
| E3 | ❌ | ✅ | `js/pwa-early.js` を `<head>` の最上部で読む形に（P1） |
| E8 | ❌ | ✅ | `offline.html` を追加（アプリと同じ配色・同じフォント）（P1） |
| E9 | ⚠️ | ✅ | `2.3.0` → `2.4.0`。2ファイルの一致を品質ゲートで見る（P1・P4） |
| E10 | ⚠️ | ✅ | MANUAL.md 2章に iOS の手順を記載（P3） |
| F1 | ⚠️ | ✅ | 画面切替の `aria-live` を追加（P1） |
| F2 | ⚠️ | ✅ | ダイアログにフォーカストラップを追加（Esc は元からあった）（P1） |
| F 外部依存 | ❌ | ✅ | Zen Maru Gothic を `fonts/` から自己ホスト（P1） |
| P2 画像 | — | ✅ | icon-512 91KB→23KB / maskable-512 75KB→22KB |

### D8 コントラストの直しかた（追加対応）

実機（Chromium）で、全画面の文字について
「実際に描画された文字色」と「重なりを解決した実際の地の色」から
コントラスト比を計算して測った。プレースホルダは疑似要素なので
`getComputedStyle(el, '::placeholder')` から、からっぽの表示は
記録0件の状態にしてから測っている。

不足していたのは `--ink-faint:#9AA7B5` の3か所だった。

| ところ | 前 | 後 |
|---|--:|--:|
| プレースホルダ「本の なまえで さがす」 | 2.45 | **5.38** |
| からっぽの文「まだ きろくが ありません」 | 2.45 | **5.38** |
| からっぽの文（紙いろの地） | 2.15 | **4.73** |

`--ink-faint` を 4.5:1 まで濃くすると、元の `--ink-soft:#5D6E80` と
ほとんど同じ濃さになってしまう（どちらも 5.3 前後）。
文字のこさの3段階が潰れるので、`--ink-soft` も一段濃くして差を残した。
`--ink-soft` も、いちばん暗い地（`--secondary-tint`）の上で **4.50 とちょうど境界**だった。

| トークン | 前 | 後 | いちばん暗い地での比 |
|---|---|---|--:|
| `--ink-soft` | `#5D6E80` | `#4C5A69` | 4.50 → **6.06** |
| `--ink-faint` | `#9AA7B5` | `#5C6C7E` | 2.11 → **4.60** |

**ブランドの6トークン（`--bg` `--panel` `--text` `--primary` `--secondary` `--accent`）は
いっさい変えていない。** 変えたのは そこから作る派生色の 文字のこさ 2つだけ。

再発しないよう、品質ゲートに D8 の検査を足した。
`css/style.css` の `:root` からトークンの値を読み、
`quality.config.json` の `contrast` に書いた組み合わせだけを計算する。
わざと元の色に戻して、5件の ❌ と 終了コード 1 で落ちることを確認済み。

## 監査時に見つかっていなかった不具合（改修中に発覚・修正済み）

**オフラインにするとアプリがまったく起動しなかった。**

サーバー（GitHub Pages を含む）は HTML や JS を gzip / brotli で縮めて返す。
`fetch` は中身をもどしてから渡してくれるが、「縮めてあります」という
`Content-Encoding` ヘッダーだけが Response に残る。
これをそのまま Cache API に入れ、圏外のときにページとして返すと、
ブラウザはヘッダーを信じてもう一度ほどこうとして失敗し、真っ白なエラー画面になる。

実機（Chromium）でサーバーを本当に止めて確認：

| | 結果 |
|---|---|
| 改修前（`a07db1f`） | `net::ERR_FAILED`（ブラウザのエラー画面） |
| 改修後 | アプリがそのまま起動する |

`sw.js` の `stripEncodingHeaders()` でヘッダーを落としてから保存するようにした。
あわせて、ページ遷移の処理を 3段（先読み → ネットワーク4秒 → キャッシュ）に整理した。
圏外だと先読みが「失敗した返事」を返すことがあり、それをそのまま画面に返していたのも
真っ白の原因だった。

## 残した項目と、その理由

品質ゲートの `securityExceptions` / `notApplicable` に、同じ内容を機械可読な形で書いてある。

| 項目 | 判定 | 理由 |
|---|:--:|---|
| CSP `style-src 'unsafe-inline'` | ✅ 解消 | `style="..."` の直書き 45か所を `css/style.css` の補助クラスに置きかえ、`style-src 'self'` にした。**main との画素差は ★ の部分のみ**（下記） |
| CSP `frame-ancestors` | 許可 | `<meta>` では効かず（ブラウザが無視して警告を出す）、GitHub Pages は応答ヘッダーを足せない。同じことを `js/pwa-early.js` で `window.top !== window.self` を見て代替 |
| maskable のセーフゾーン | ✅ 問題なし（**前回の指摘は誤り**） | 下記「訂正」を参照 |
| `--ink-faint: #9AA7B5` | 報告のみ | 白背景の小さい文字に使うとコントラストが 2.4:1 で 4.5:1 に届かない。改修モードの規約「配色を変更しない」に当たるため、**別PRで扱う提案にとどめる**。使用箇所は補助的な説明文とプレースホルダで、意味を色だけで伝えている箇所はない |
| ★（ほし）の色 | ✅ 解消 | 金色（`--accent`）はそのままに、ほしの形に `--text` の輪郭（白地で 15:1）を付けた。あわせて ついていない ほしを **中ぬき** にして、ついている／いない を「色だけ」で伝えないようにした（Part I §2-8） |
| 提示モード | 対象外 | 一人ひとりの読書記録アプリで、一斉授業での提示を前提としない。個人の読書履歴を電子黒板に映すことは個人情報の観点からも推奨されない |
| `manifest.json` のファイル名 | 現状維持 | 規約は `manifest.webmanifest` だが、既にインストール済みの端末が参照しているため改名しない |
| B3 B5 C1 | 対象外 | サーバー（GAS）を持たない A型 |

## 人間に確認してほしいこと

1. **アイコンの画質**（icon-512 91.3KB → 23.4KB ／ icon-maskable-512 74.9KB → 21.9KB）
   128色のパレットに落としてある。元画像との差は RMSE 4.9（256段階中）で、
   アイコンとして表示される大きさでは見分けがつかないが、最終判断はお願いしたい。
   元のファイルはこのコミットの1つ前（`0ef82c1^`）から取り出せる。
2. **maskable アイコンの絵がら**を 80% セーフゾーンに収めるかどうか
3. **★（ほし）の色** — 下記のとおり、文字ではないので今回は変えていない


---

# 🔧 訂正：maskable アイコンのセーフゾーンについて

**前回の監査で「絵がらが中央80%のセーフゾーンをはみ出している」と書いたのは誤りでした。**

まるく切りぬいた絵を目で見くらべて判断したためです。実際に画素を測ると、
絵のいちばん外側の点は **中心から 193px**（512px 版）で、
許容される **205px**（＝ 512 × 0.4）の内側に収まっていました。修正は不要です。

いったん絵を80%に縮める修正を入れましたが、**必要のない縮小だったので取り消しました。**
（そのまま入れると、ホーム画面のアイコンが 20% 小さく見えるだけの改悪になっていました）

同時に、この検査を `tests/browser.test.mjs` に入れました。
ここで **もうひとつ間違えた**ので書き残します。

- 最初は「上下左右10%を除いた**正方形**」に収まっているかで見ていた
- しかし セーフゾーンは **まる**（直径80%）。正方形の かど は まるの外にはみ出す
- そのため検査がゆるくなり、**修正前のアイコンでも通ってしまった**
- 中心からの距離（半径）で見るように直した

セーフゾーンを四角で検査してはいけません。

---

# 🔒 CSP を `'unsafe-inline'` なしにした

`style="..."` の直書きを、`index.html` で 27か所、`js/app.js` で 18か所、
`css/style.css` の補助クラス（`.mt-4` `.txt-danger` `.field-label` など）に置きかえ、
`style-src` を `'self'` だけにしました。

大きさがそのつど変わるところは、クラスでは表せないので次のようにしています。

| ところ | やりかた |
|---|---|
| グラフの棒の高さ | `data-h` に％を持たせ、置いたあとに `element.style.height` で伸ばす |
| 進み具合のバー | もともと `bar.style.width` で動かしていた（そのまま） |
| ★ の大きさ | `'30px'` と `'34px'` の2種類しかなかったので `.stars-md` / `.stars-lg` に |

`element.style` の書きかえは CSSOM の操作なので CSP の対象外です。

**見た目が変わっていないことの確認**：`main` と改修後で、同じ記録を入れて
全画面（ホーム・リスト・スタンプ・きろく・ふりかえり・データ・つかいかた・
ぜんぶけす・本のくわしく・印刷）を撮って画素をくらべました。

| 画面 | ちがう画素 |
|---|--:|
| スタンプ / きろく / 印刷 / 下位4画面 | **0.000%** |
| ホーム | 0.126% |
| リスト | 0.213% |
| 本のくわしく | 0.348% |

差が出た3画面は **すべて ★ の部分だけ**で、余白も文字位置も1画素も動いていません
（差分を赤く塗った画像で、変化が ★ の矩形に限られることを確認）。

---

# 🧪 検査を くり返せる形にした

これまでの確認は使い捨てのスクリプトで行っており、残っていませんでした。
`tests/browser.test.mjs` として リポジトリに入れ、`npm test` で走ります。

とくに **「サーバーを本当に止めてアプリが起動するか」** は、
以前の `Content-Encoding` の不具合が再発したときに ここで捕まります。
ブラウザの「オフライン疑似」は ページ遷移を Service Worker より手前で止めてしまい、
キャッシュから返せているかを確かめられないため、実際にサーバーを落としています。

検査そのものが空振りしていないかを見る項目も入れました。
検査用の記録の日付を固定日にしていたため、
「よんだ本の リスト」の既定の絞りこみ（“この月”）から外れて 1冊も表示されず、
本の行・★・かんそう をまったく見ないまま ✅ になっていたのを、途中で見つけたためです。
